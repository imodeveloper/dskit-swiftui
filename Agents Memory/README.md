# Agents Memory

Long-lived engineering memory and reference documents for `dskit-swiftui`.

## Current documents
- `ARCHITECTURE.md`
- `PROJECT_ANALYSIS.md`
- `File Changes/README.md`
- `CHANGELOG.md` (legacy chronological memory)

## Usage
- Keep architecture notes, implementation audits, and design-system learnings here.
- Update `/Users/ivan.borinschi/Work/dskit-swiftui/AGENTS.md` when adding or relocating key memory files.
- Use `File Changes/files/` as the primary required per-file rationale memory before commit + push. Search it by repo-relative path before editing a file.
- Keep each per-file memory under 300 lines with newest entries at the top.
- Use `CHANGELOG.md` only for legacy lookup or explicit release/global summaries.
