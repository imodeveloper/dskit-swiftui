# DSThread

> Generated by `Scripts/documentation_generator.sh`. Edit the Swift source comment or generator instead of this file.

## Reference

- Source: [DSKit/Sources/DSKit/Views/DSThread.swift](../../DSKit/Sources/DSKit/Views/DSThread.swift)
- Full usage map: [UsageIndex.md#dsthread](UsageIndex.md#dsthread)
- Explorer usage: 0 screen files
- Snapshot: not available

## Overview

`DSThread` is a DSKit SwiftUI view. Add source documentation to expand this generated reference.

## Example

```swift
struct Testable_DSThread: View {

    let colors: [ThreadItem] = [
        ThreadItem(item: SomeColor(title: "red", color: Color.red)),
        ThreadItem(item: SomeColor(title: "green", color: Color.green)),
        ThreadItem(item: SomeColor(title: "yellow", color: Color.yellow)),
        ThreadItem(item: SomeColor(title: "purple", color: Color.purple))
    ]

    var body: some View {
        ScrollView {
            DSThread(
                data: colors,
                id: \.self,
                header: { threadItem, _ in
                    DSHStack {
                        Circle()
                            .fill(threadItem.item.color)
                            .frame(width: 24, height: 24)

                        Text(threadItem.item.title)
                    }
                }, content: { threadItem, _ in
                    threadItem.item.color
                        .frame(height: 100)
                }, footer: {
                    Color.red.frame(height: 100)
                }
            ).environment(\.showFooterThreadLine, true)
        }
    }
}
```

## Snapshot

No component snapshot image is available yet.

## DSKitExplorer Usage

No direct `DSKitExplorer/Screens` usage was found.

## Related Components

[DSHStack](DSHStack.md), [DSList](DSList.md), [DSText](DSText.md), [DSVStack](DSVStack.md)
